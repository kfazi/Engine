SET(RENDERER_NAME "directx_renderer")
SET(RENDERER_SOURCES "")
SET(RENDERER_DEFINITIONS "")
SET(RENDERER_INCLUDE_DIR "")
SET(RENDERER_LIBRARIES "")

SET(RENDERER_DIRECTX_SRC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/renderers/directx" CACHE STRING "DirectX renderer source directory")

SOURCE_GROUP("" FILES "CMakeListsRendererDirectX.txt")
LIST(APPEND RENDERER_SOURCES "CMakeListsRendererDirectX.txt")

SET(RENDERER_SOURCES
	${RENDERER_DIRECTX_SRC_DIR}/main.cpp
)
SOURCE_GROUP("src" FILES ${RENDERER_SOURCES})

LIST(APPEND RENDERER_DEFINITIONS ${COMMON_DEFINITIONS})
LIST(APPEND RENDERER_INCLUDE_DIR ${ENGINESRC_DIR})
LIST(APPEND RENDERER_INCLUDE_DIR ${RENDERER_DIRECTX_SRC_DIR})
LIST(APPEND RENDERER_INCLUDE_DIR ${Boost_INCLUDE_DIR})
#LIST(APPEND RENDERER_LIBRARIES ${FRAMEWORK_SYSTEM_LIBRARIES})

INCLUDE_DIRECTORIES(${RENDERER_INCLUDE_DIR})
LINK_DIRECTORIES(${RENDERER_LIBRARIES})

ADD_LIBRARY(${RENDERER_NAME} MODULE ${RENDERER_SOURCES})
SET_PROPERTY(TARGET ${RENDERER_NAME} PROPERTY COMPILE_DEFINITIONS ${RENDERER_DEFINITIONS})
TARGET_LINK_LIBRARIES(${RENDERER_NAME} ${RENDERER_LIBRARIES})
ADD_DEPENDENCIES(${RENDERER_NAME} ${ENGINE_NAME})

# EOF
