SET(FRAMEWORK_SOURCES "")
SET(FRAMEWORK_DEFINITIONS "")
SET(FRAMEWORK_INCLUDE_DIR "")
SET(FRAMEWORK_LIBRARIES "")

SOURCE_GROUP("" FILES "CMakeListsFramework.txt")
LIST(APPEND FRAMEWORK_SOURCES "CMakeListsFramework.txt")

SET(FRAMEWORK_BASE_SOURCES
	${FRAMEWORKSRC_DIR}/base/enginemainimplementation.cpp
	${FRAMEWORKSRC_DIR}/base/enginemainimplementation.hpp
	${FRAMEWORKSRC_DIR}/base/loadengine.cpp
	${FRAMEWORKSRC_DIR}/base/loadengine.hpp
	${FRAMEWORKSRC_DIR}/base/main.cpp
	${FRAMEWORKSRC_DIR}/base/winmain.cpp
)
SOURCE_GROUP("src\\base" FILES ${FRAMEWORK_BASE_SOURCES})
LIST(APPEND FRAMEWORK_SOURCES ${FRAMEWORK_BASE_SOURCES})

LIST(APPEND FRAMEWORK_DEFINITIONS ${COMMON_DEFINITIONS})
LIST(APPEND FRAMEWORK_INCLUDE_DIR ${ENGINESRC_DIR})
LIST(APPEND FRAMEWORK_INCLUDE_DIR ${FRAMEWORKSRC_DIR})
#LIST(APPEND FRAMEWORK_INCLUDE_DIR ${OIS_INCLUDE_DIR})
#LIST(APPEND FRAMEWORK_INCLUDE_DIR ${Boost_INCLUDE_DIR})
LIST(APPEND FRAMEWORK_LIBRARIES ${FRAMEWORK_SYSTEM_LIBRARIES})

#SET(FRAMEWORK_DEFINITIONS ${FRAMEWORK_DEFINITIONS} CACHE INTERNAL "")
#SET(FRAMEWORK_INCLUDE_DIR ${FRAMEWORK_INCLUDE_DIR} CACHE INTERNAL "")
#SET(FRAMEWORK_SOURCES ${FRAMEWORK_SOURCES} CACHE INTERNAL "")
#SET(FRAMEWORK_LIBRARIES ${FRAMEWORK_LIBRARIES} CACHE INTERNAL "")

INCLUDE_DIRECTORIES(${FRAMEWORK_INCLUDE_DIR})
LINK_DIRECTORIES(${FRAMEWORK_LIBRARY_DIR})

ADD_EXECUTABLE(${GAME_NAME} WIN32 ${FRAMEWORK_SOURCES})
SET_PROPERTY(TARGET ${GAME_NAME} PROPERTY COMPILE_DEFINITIONS ${FRAMEWORK_DEFINITIONS})
TARGET_LINK_LIBRARIES(${GAME_NAME} ${FRAMEWORK_LIBRARIES})
ADD_DEPENDENCIES(${GAME_NAME} ${ENGINE_NAME})

# EOF
